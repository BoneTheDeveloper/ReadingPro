
/* !!! This is code generated by Prisma. Do not edit directly. !!! */
/* eslint-disable */
// biome-ignore-all lint: generated file
// @ts-nocheck 
/*
 * This file should be your main import to use Prisma. Through it you get access to all the models, enums, and input types.
 * If you're looking for something you can import in the client-side of your application, please refer to the `browser.ts` file instead.
 *
 * 🟢 You can import this file directly.
 */

import * as process from 'node:process'
import * as path from 'node:path'
import { fileURLToPath } from 'node:url'
globalThis['__dirname'] = path.dirname(fileURLToPath(import.meta.url))

import * as runtime from "@prisma/client/runtime/client"
import * as $Enums from "./enums"
import * as $Class from "./internal/class"
import * as Prisma from "./internal/prismaNamespace"

export * as $Enums from './enums'
export * from "./enums"
/**
 * ## Prisma Client
 * 
 * Type-safe database client for TypeScript
 * @example
 * ```
 * const prisma = new PrismaClient({
 *   adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL })
 * })
 * // Fetch zero or more DictionaryEntries
 * const dictionaryEntries = await prisma.dictionaryEntry.findMany()
 * ```
 * 
 * Read more in our [docs](https://pris.ly/d/client).
 */
export const PrismaClient = $Class.getPrismaClientClass()
export type PrismaClient<LogOpts extends Prisma.LogLevel = never, OmitOpts extends Prisma.PrismaClientOptions["omit"] = Prisma.PrismaClientOptions["omit"], ExtArgs extends runtime.Types.Extensions.InternalArgs = runtime.Types.Extensions.DefaultArgs> = $Class.PrismaClient<LogOpts, OmitOpts, ExtArgs>
export { Prisma }

/**
 * Model DictionaryEntry
 * A dictionary headword (one term), shared across all users. Client: lookup
 * result header; entries rank by frequency for suggestions.
 */
export type DictionaryEntry = Prisma.DictionaryEntryModel
/**
 * Model DictionarySense
 * One sense (meaning) of an entry. Client: each definition block under a result.
 */
export type DictionarySense = Prisma.DictionarySenseModel
/**
 * Model DictionaryTranslation
 * A translation of a sense into a target language, with provenance + ranking.
 * Client: translated meaning(s) under a sense (the primary one highlighted).
 */
export type DictionaryTranslation = Prisma.DictionaryTranslationModel
/**
 * Model DictionaryAlias
 * An alternate lookup form (inflection/variant/misspelling) mapping to an entry.
 * Server-side: lets lookups resolve non-headword inputs to the right entry.
 */
export type DictionaryAlias = Prisma.DictionaryAliasModel
/**
 * Model DictionarySourceAudit
 * Audit trail for dictionary seed/import batches. Server/admin only; records
 * which batch produced which entity for traceability.
 */
export type DictionarySourceAudit = Prisma.DictionarySourceAuditModel
/**
 * Model Passage
 * A reading passage: the source text a learner studies, created by paste (TEXT)
 * or PDF extraction. Client: rows in the study sources panel; opening one loads
 * the study workspace (content + studio).
 */
export type Passage = Prisma.PassageModel
/**
 * Model StudyChatMessage
 * One message in the per-passage AI chat thread. Client: studio chat panel.
 */
export type StudyChatMessage = Prisma.StudyChatMessageModel
/**
 * Model StudioArtifact
 * A studio output card (a quiz or flashcard set) generated from a passage.
 * Stays generic across types; type-specific rows attach via relations.
 * Client: studio panel cards; status drives the generating/ready/error state.
 */
export type StudioArtifact = Prisma.StudioArtifactModel
/**
 * Model Question
 * A single quiz question belonging to a quiz artifact. Client: studio quiz
 * content (question text, options, grading, explanation).
 */
export type Question = Prisma.QuestionModel
/**
 * Model QuizResult
 * Graded outcome of a quiz run, 1:1 with the quiz StudioArtifact. Client: studio
 * quiz results screen; also aggregated into the progress dashboard.
 */
export type QuizResult = Prisma.QuizResultModel
/**
 * Model StudySession
 * One study session: tracks who is studying and a presence window updated by a
 * client heartbeat. Open while completedAt is null. Client: drives the active-
 * session indicator and feeds time-studied stats on the progress dashboard.
 */
export type StudySession = Prisma.StudySessionModel
/**
 * Model TranslationCache
 * Cached translation of a selection, keyed by `cacheKey` to avoid re-calling the
 * provider. Client: instant result when the same selection is looked up again.
 */
export type TranslationCache = Prisma.TranslationCacheModel
/**
 * Model TranslationHistory
 * Append-only log of translations the learner performed. Client: translation
 * history list; unlike the cache, every lookup is recorded.
 */
export type TranslationHistory = Prisma.TranslationHistoryModel
/**
 * Model FileUploadIntent
 * A pending upload authorization: grants temporary, expiring access to a private
 * Blob pathname before the file becomes a Passage. Server-only; consumed/expired
 * after the upload completes. Client: backs the upload flow's signed access.
 */
export type FileUploadIntent = Prisma.FileUploadIntentModel
/**
 * Model UserProfile
 * Learner account, keyed by the Clerk user id (text, not a generated UUID).
 * Owns every user-scoped record. Client: profile/account page, tier gating, and
 * the implicit owner of study, vocabulary, and upload data.
 */
export type UserProfile = Prisma.UserProfileModel
/**
 * Model VocabularyItem
 * A saved vocabulary entry the learner collected (from translate or dictionary),
 * carrying spaced-repetition state. Client: vocabulary list rows + SRS review.
 */
export type VocabularyItem = Prisma.VocabularyItemModel
/**
 * Model VocabularyOccurrence
 * A specific place a vocabulary item was encountered (passage + sentence). One
 * item can have many occurrences. Client: "seen in" context on a word.
 */
export type VocabularyOccurrence = Prisma.VocabularyOccurrenceModel
/**
 * Model VocabularySet
 * A named collection of vocabulary items. MANUAL = user list; DAILY/WEEKLY =
 * auto-built review batches bounded by periodStart/periodEnd. Client: sets tabs.
 */
export type VocabularySet = Prisma.VocabularySetModel
/**
 * Model VocabularySetItem
 * Join row placing a vocabulary item into a set. Client: set membership.
 */
export type VocabularySetItem = Prisma.VocabularySetItemModel
