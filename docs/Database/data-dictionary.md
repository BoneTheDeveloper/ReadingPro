# Data Dictionary

**English Reading Training App - Prisma Schema**

## Identifier Standard

| Identifier kind | PostgreSQL type | Prisma declaration |
|-----------------|-----------------|--------------------|
| Clerk profile ID | `text` | `String @id` |
| Application-owned primary key | `uuid` | `String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid` |
| Relation/entity-reference ID for app-owned entities | `uuid` | `String @db.Uuid` |
| Relation ID to `profiles.id` | `text` | `String` |
| Product/cache/UI key | `text` | `String` |

Application-owned public table primary keys, foreign keys, and persisted
entity-reference IDs use native PostgreSQL UUIDs. `profiles.id` stores the
Clerk user id as text, so user relations such as `Passage.userId` and
`CardReview.userId` are text. Product keys such as `cacheKey`, `normalizedKey`,
`stripeCustomerId`, Blob pathnames, and question option IDs remain strings.

## Enums

| Enum | Values |
|------|--------|
| `CEFRLevel` | `A1`, `A2`, `B1`, `B2`, `C1`, `C2` |
| `SourceType` | `TEXT`, `PDF` |
| `QuestionType` | `MULTIPLE_CHOICE`, `TRUE_FALSE` |
| `Tier` | `FREE`, `PRO` |

## User And Study Tables

### UserProfile (`profiles`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | String | PK, supplied by Clerk user id |
| `email` | String? | Unique |
| `name`, `avatarUrl`, `bio` | String? | Optional profile metadata |
| `targetLevel` | CEFRLevel | Default `B2` |
| `tier` | Tier | Default `FREE` |
| `stripeCustomerId` | String? | Unique external billing ID |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

### Passage (`passages`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `userId` | String | FK -> `profiles.id` |
| `title`, `content` | String | Learner passage |
| `simplifiedContent` | String? | Optional simplified text |
| `originalLevel`, `simplifiedLevel` | CEFRLevel? | Optional CEFR analysis |
| `wordCount` | Int | Original content word count |
| `sourceType` | SourceType | `TEXT` or `PDF` |
| `filePath` | String? | Unique local/Vercel Blob pathname |
| `deletedAt` | DateTime? | Soft-delete timestamp |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

### StudyChatMessage (`study_chat_messages`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `userId` | String | FK -> `profiles.id` |
| `passageId` | UUID | FK -> `passages.id` |
| `role`, `content` | String | Chat message role and content |
| `createdAt` | DateTime | Creation timestamp |

### Question (`questions`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `passageId` | UUID | FK -> `passages.id` |
| `questionText`, `sourceText`, `explanation` | String | Question content |
| `options` | Json | UI option objects; option IDs remain strings |
| `correctOption` | String | UI option ID, not a public entity ID |
| `sourceLine` | Int | Source line number |
| `questionType` | QuestionType | Default `MULTIPLE_CHOICE` |
| `difficulty` | Int | Default `3` |
| `createdAt` | DateTime | Creation timestamp |

### CardReview (`card_reviews`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `questionId` | UUID | FK -> `questions.id` |
| `userId` | String | FK -> `profiles.id` |
| `qualityRating` | Int | SM-2 quality rating |
| `easeFactor`, `intervalDays`, `repetitions` | Float / Int | SM-2 state |
| `nextReviewDate`, `reviewedAt` | DateTime | Scheduling timestamps |

Unique: `[questionId, userId]`.

### StudySession (`study_sessions`)

Tracks study engagement: who started a session, when it began, when it was last
seen active, and when it completed. Quiz performance stats live in
`QuizAttempt`.

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `userId` | String | FK -> `profiles.id` |
| `startedAt`, `completedAt`, `lastActivityAt` | DateTime / DateTime? / DateTime | Session timestamps and activity heartbeat |

Partial unique index `study_sessions_one_open_per_user` on `(userId) WHERE completedAt IS NULL`
enforces at most one open session per user (backstop to the per-user advisory lock in
`ensureActiveSession`).

### QuizAttempt (`quiz_attempts`)

Source of truth for quiz performance. Each record is one quiz run: how many
questions, how many correct, computed accuracy. It is a child record of
`StudySession` and carries the passage context for that run.

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `studySessionId` | UUID | FK -> `study_sessions.id`, cascade on delete |
| `userId` | String | FK -> `profiles.id` |
| `passageId` | UUID? | Nullable FK -> `passages.id`, set null when passage is deleted |
| `correctCount` | Int | Default 0, correct answers in this attempt |
| `incorrectCount` | Int | Default 0, incorrect answers in this attempt |
| `totalQuestions` | Int | Default 0, total questions in this attempt |
| `accuracyRate` | Float? | Computed on completion: `correctCount / totalQuestions * 100` |
| `startedAt` | DateTime | Default `now()`, when attempt started |
| `completedAt` | DateTime? | Nullable, set when attempt completed |

Index: `[userId, startedAt]`.

### FileUploadIntent (`file_upload_intents`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `userId` | String | FK -> `profiles.id` |
| `pathname` | String | Unique local/Vercel Blob pathname |
| `expiresAt` | DateTime | Expiration deadline for private upload access |
| `createdAt` | DateTime | Creation timestamp |

## Translation Tables

### TranslationCache (`translation_caches`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `cacheKey` | String | Unique cache key |
| `userId` | String | FK -> `profiles.id` |
| `sourceId` | UUID | FK -> `passages.id` |
| `selectedText`, `contextSentence` | String | Translation input |
| `sourceLanguage`, `targetLanguage` | String | Language codes |
| `mode`, `provider` | String | Required legacy/internal values |
| `response` | Json | Cached response payload |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

### TranslationHistory (`translation_histories`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `userId` | String | FK -> `profiles.id` |
| `sourceId` | UUID | FK -> `passages.id` |
| `selectedText`, `contextSentence`, `translation` | String | Translation record |
| `sourceLanguage`, `targetLanguage` | String | Language codes |
| `mode`, `provider` | String | Required legacy/internal values |
| `response` | Json | Full response payload |
| `createdAt` | DateTime | Creation timestamp |

### VocabularyItem (`vocabulary_items`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `normalizedKey` | String | Unique product key |
| `userId` | String | FK -> `profiles.id` |
| `sourceId` | UUID | FK -> `passages.id` |
| `selectedText`, `translation`, `contextSentence` | String | Saved vocabulary content |
| `sourceLanguage`, `targetLanguage` | String | Language codes |
| `type` | String? | Optional word/phrase type |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

## Dictionary Tables

### DictionaryEntry (`dictionary_entries`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `headword`, `normalizedHeadword` | String | Display and lookup forms |
| `sourceLanguage` | String | Default `en` |
| `frequencyRank` | Int | Default `0` |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

Unique: `[normalizedHeadword, sourceLanguage]`.

### DictionarySense (`dictionary_senses`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `entryId` | UUID | FK -> `dictionary_entries.id` |
| `partOfSpeech`, `definition`, `example` | String? | Sense content |
| `tags` | String[] | Sense tags |
| `usageRank` | Int | Default `0` |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

### DictionaryTranslation (`dictionary_translations`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `senseId` | UUID | FK -> `dictionary_senses.id` |
| `targetLanguage`, `translation` | String | Translation content |
| `isPrimary`, `rank`, `confidence` | Boolean / Int / Float? | Ranking data |
| `status`, `sourceType`, `sourceName` | String / String / String? | Provenance |
| `reviewedAt` | DateTime? | Review timestamp |
| `createdAt`, `updatedAt` | DateTime | Creation/update timestamps |

### DictionaryAlias (`dictionary_aliases`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `entryId` | UUID | FK -> `dictionary_entries.id` |
| `normalizedAlias` | String | Normalized lookup alias |
| `aliasType` | String | Default `variant` |

Unique: `[normalizedAlias, entryId]`.

### DictionarySourceAudit (`dictionary_source_audits`)

| Column | Type | Constraints / purpose |
|--------|------|-----------------------|
| `id` | UUID | PK, generated by database |
| `batchName`, `entityType` | String | Seed/import provenance |
| `entityId` | UUID | Persisted public entity reference |
| `note` | String? | Optional audit note |
| `createdAt` | DateTime | Creation timestamp |

**Last Updated:** 2026-06-08
