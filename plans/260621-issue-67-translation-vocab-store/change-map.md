# Change Map — Issue #67 (manual follow-along guide)

A flat, ordered list of **every file you'll touch**, what changes in it, and the
**related files** to read alongside. Follow top to bottom. Detail lives in the
`phase-*.md` files; this is the checklist view.

Legend: ✏️ modify · ➕ create · ❌ delete · 👁️ read-only (context)

---

## Phase 1 — Response Contract DTO (fix silent save + stop field leak)

| | File | What |
|---|------|------|
| ✏️ | `src/app/api/vocabulary/route.ts` | Add `toVocabularyDTO(item)` mapper; return `{ success: true, data: toVocabularyDTO(item) }` instead of raw `item`. Use `new Date(item.createdAt).toISOString()` + `item.type` (no `?? null`). |
| ✏️ | `tests/vitest/integration/api/vocabulary-save-route.test.ts` | Add: success response parses `vocabularyResponseSchema`; `data` has exactly the 6 DTO keys. Make the mocked service return a **full-row** fixture so the mapper is exercised. |
| ➕ | unit test for `toVocabularyDTO` | Feed a full Prisma-shaped object (extra fields + `Date` dates) → assert exactly 6 keys, ISO strings. (Co-locate with the route test or a small util test.) |

**Related (read-only):**
- 👁️ `src/contracts/translation/translation-response-schema.ts` — `vocabularyDataSchema` (the target shape; `.strict()`).
- 👁️ `src/server/modules/vocabulary/vocabulary.service.ts` — `saveVocabularyItem` returns the domain `VocabularyItem` (keep it returning the model).
- 👁️ `tests/vitest/fixtures/vocabulary.ts` — `vocabularyItemFixture` (full-row shape).
- 👁️ `src/features/study/ui/study-workspace-client.tsx:254` — confirm client reads only `.data.id` (consumer check, M4).

**Gate:** `pnpm run typecheck` + save-route test green.

---

## Phase 2 — Dedup Store Strategy (normalize the translation key)

| | File | What |
|---|------|------|
| ✏️ | `prisma/schema/vocabulary.prisma` | Add `normalizedTranslation String` (required, no default). Change `@@unique([userId, normalizedText, targetLanguage, translation])` → `@@unique([userId, normalizedText, targetLanguage, normalizedTranslation])`. |
| ✏️ | `src/server/db/vocabulary-queries.ts` | In `upsertVocabularyItem`: compute `normalizedTranslation = normalizeText(params.translation)`; set it in `create`; change the upsert `where` key to use it. `update` branch unchanged (savedCount++, preserve SRS). |
| ❌ | `src/server/db/translation-queries.ts` | Delete the dead `saveVocabularyItem` (lines ~123–184) + any now-unused local helpers. No callers; it keys on the **old** composite name and would break typecheck after the rename. |
| ➕ | `prisma/migrations/<ts>_vocabulary_normalized_translation_key/` | Generated by `pnpm run db:migrate:dev`. Plain column + unique change — **no custom SQL**. |
| ➕ | normalization/key unit test | Pure test: `"Chạy"`/`"chạy"`/`"  chạy  "` normalize equal; `"chạy"` ≠ `"chạy bộ"`. No DB. |

**Related (read-only):**
- 👁️ `src/server/db/vocabulary-queries.ts:11-13` — `normalizeText` (reuse it; single source of truth).
- 👁️ `src/server/modules/vocabulary/vocabulary.service.ts` — orchestrates ownership + calls `upsertVocabularyItem`.

**Migration steps:** edit schema → `pnpm run db:migrate:dev` → `prisma migrate reset` (dev DB is disposable) → regenerate client → `pnpm run typecheck`.

**Manual dev-DB verification (replaces a real-DB test harness):**
- [ ] Save "run"→"chạy" twice → 1 item, `savedCount = 2`.
- [ ] "run"→"chạy" then "run"→"chạy bộ" → 2 items.
- [ ] "run"→"Chạy" then "run"→"chạy" → 1 item.
- [ ] Same item, two `sourceId`s → 1 item, 2 occurrences.

---

## Phase 3 — Contract Regression + Telemetry + Docs

| | File | What |
|---|------|------|
| ✏️ | `tests/vitest/integration/api/vocabulary-save-route.test.ts` | Contract regression (6-key DTO parse); validation 400 / auth 401 / TRANSLATE-ownership 404. |
| ✏️ | `tests/vitest/integration/api/translation-vocabulary-routes.test.ts` | Keep envelope/contract coverage aligned with the DTO. |
| ✏️ | `docs/API/Routes/response-contract-coverage.md` | Update the Phase 2 Contract Test Map row for `POST /api/vocabulary`. |
| ✏️ | this plan + `brainstorm-summary.md` | M3 client saved-state limitation noted (already drafted in Phase 3 doc). |

**Related (read-only):**
- 👁️ `tests/vitest/mocks/logger.ts` — spy target for the telemetry "no raw text" assertion.
- 👁️ `tests/vitest/setup/vitest.setup.ts` — confirms `db` is globally mocked (why dedup isn't asserted here).

**Gate:** `pnpm run typecheck && pnpm run lint && pnpm run test` green.

---

## Deferred to the UI round (NOT in this plan)

- 👁️ `src/features/study/ui/studio/translate/translation-popup.tsx` — add Save button.
- 👁️ `src/features/study/ui/study-workspace-client.tsx` — in-flight guard; **re-key `savedVocabularyIds` on the returned item `id`** (fixes the M3 cross-passage Saved-badge divergence).
- Dictionary FK ownership validation on the `DICTIONARY` save path.

---

## One-glance file list

```
Phase 1: route.ts(✏️) + vocabulary-save-route.test.ts(✏️) + toVocabularyDTO unit test(➕)
Phase 2: vocabulary.prisma(✏️) + vocabulary-queries.ts(✏️) + translation-queries.ts(❌) + migration(➕) + normalize unit test(➕)
Phase 3: vocabulary-save-route.test.ts(✏️) + translation-vocabulary-routes.test.ts(✏️) + response-contract-coverage.md(✏️)
```
